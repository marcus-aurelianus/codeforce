import sys
reader = (s.rstrip() for s in sys.stdin)
input = reader.__next__
from collections import defaultdict

curr = defaultDict)
curr = {(1,1):0}
n,k = list(map(int,input().split())) 

for i in range(n):
    t,x,y = list(map(int,input().split())) 
    for diu in curr:
        x0, y0 = diu
        if t<= abs(x-x0)+abs(y-y0):
            dic



            


#"{} {} {}".format(maxele,minele,minele)
